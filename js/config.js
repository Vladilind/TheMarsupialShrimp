// Global constants
const CANVAS_WIDTH = 600;
const CANVAS_HEIGHT = 400;
const PLANKTON_SIZE = 10;
const BASE_PARALLAX_SCROLL_SPEED = 0.5;

// Player specific constants (from player object in gameSetup.js)
// const PLAYER_SPEED = 4; // This was part of the player object, might be better to keep it there or make it configurable.
// const PLAYER_INVULNERABILITY_DURATION = 120; // frames, not ms.

// Constants derived from ENEMY_TYPES can remain in enemyService.js or be explicitly defined here if truly global.
// For now, let's keep them closer to their usage.
